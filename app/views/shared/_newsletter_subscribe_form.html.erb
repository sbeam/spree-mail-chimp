          <% form_for :subscriber, :url => '/newsletter' do |f| %>
            <table>
              <tr>
                <td>
                  <%= text_field_tag :email, t('enter_your_email'), :class=>'email', :id=>'subscribe_email' %>
                </td>
                <td>
                  <%= submit_tag t('submit'), :type=>'image',:src=>'/images/btn.submit1.gif',:id => 'op_subscribe' %>
                </td>
              </tr>
            </table>
          <% end %>
